# GitHub Integration Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
FITREP_DATA=ghp_your_github_personal_access_token_here

# Repository Dispatch Token for account creation (repo:dispatch)
# Typically same scopes as above, but can be fine-grained with repository_dispatch access
DISPATCH_TOKEN=ghp_your_repository_dispatch_token_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Session Security
# Generate a random string for production: openssl rand -base64 32
SESSION_SECRET=change_this_to_a_random_string_in_production

# Client Origin (for CORS)
CLIENT_ORIGIN=http://localhost:8080

# Optional: GitHub Configuration Override
# GITHUB_REPO_OWNER=SemperAdmin
# GITHUB_REPO_NAME=Fitness-Report-Evaluator-Data
# MAIN_REPO=SemperAdmin/Fitness-Report-Evaluator
# DATA_REPO=SemperAdmin/Fitness-Report-Evaluator-Data
